
<div class="wrapper">
    <div class="container">
      <div class="select"><h6>Trier en fonction de :</h6>
          <%= simple_form_for :query, url: issues_path, method: :get do |f| %>
          <%= f.input :fix_status, as: :radio_buttons, collection: {'Par statut' => :fix_status, 'Par vote' => :vote_count, 'Par date' => :date}, placeholder: 'résolu', label: '' %>
          <%= f.button :submit %>
      <% end %>
    </div>
      <div class="card-display" id="cards" id="left-screen">
        <div class="row">
          <% @issues.each do |issue| %>
          <div class="col-xs-12 col-sm-12 col-md-6">
            <%= render 'card', issue: issue %>
          </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
    <div
    id="stickymap"
    data-mkers="<%= @mkers.to_json %>"
    > </div>

</div>
